@typeparam T

<MudToggleItem T="T"
               Value="Value"
               Class="@GetShapeClass()"
               Style="@GetComputedStyle()"
               title="@Title"
               aria-label="@Title">
    @ChildContent
</MudToggleItem>
